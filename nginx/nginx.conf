events {
}

http {
    upstream fred {
        server mapd-00:9090;
        server mapd-01:9090;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://fred;
        }
    }
}
