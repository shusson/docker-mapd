events {
}

http {
    upstream fred {
        ip_hash;
        server mapd-api:4000;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://fred;
        }
    }
}
